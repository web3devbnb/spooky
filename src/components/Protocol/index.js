import * as React from "react"
import { Link } from "gatsby"
import * as styles from "./index.module.scss"

const Protocol = () => (
  <section className={styles.protocol}>
    <h3>Rentero Protocol.</h3>

    <div></div>
  </section>
)

export default Protocol
